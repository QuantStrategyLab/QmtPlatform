[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qmt-platform"
version = "0.1.0"
description = "QuantStrategyLab platform layer for A-share strategies via miniQMT (dry-run first)."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "QuantStrategyLab" }]
dependencies = [
  "flask>=3.0",
  "pandas>=2.0",
  "quant-platform-kit @ git+https://github.com/QuantStrategyLab/QuantPlatformKit.git@be45863ad75316e3c993338828da356ddfb42f65",
  "cn-equity-strategies @ git+https://github.com/QuantStrategyLab/CnEquityStrategies.git@38c0360a4e98aa4851280a90dc1d42a18b256e8f",
]

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.setuptools]
py-modules = [
  "main",
  "runtime_preflight",
  "runtime_config_support",
  "strategy_loader",
  "strategy_registry",
  "strategy_runtime",
]

[tool.setuptools.packages.find]
include = ["application*"]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.pytest.ini_options]
pythonpath = ["."]
