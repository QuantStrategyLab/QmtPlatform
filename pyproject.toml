[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qmt-platform"
version = "0.1.0"
description = "QuantStrategyLab platform layer for A-share strategies via miniQMT (dry-run first)."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "QuantStrategyLab" }]
dependencies = [
  "flask>=3.0",
  "pandas>=2.0",
  "quant-platform-kit @ git+https://github.com/QuantStrategyLab/QuantPlatformKit.git@aae333fe8b3fe5aeb32e1ff135ab14ea7db32420",
  "cn-equity-strategies @ git+https://github.com/QuantStrategyLab/CnEquityStrategies.git@b2a76d30935073b2c54a3ca9871c27827f110497",
]

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.setuptools]
py-modules = [
  "main",
  "runtime_preflight",
  "runtime_config_support",
  "strategy_loader",
  "strategy_registry",
  "strategy_runtime",
]

[tool.setuptools.packages.find]
include = ["application*"]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.pytest.ini_options]
pythonpath = ["."]
